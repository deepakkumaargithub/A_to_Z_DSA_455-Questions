
class Solution {

    void printTriangle(int n) {
        for(int row=n; row>=1; row--){
            char ch = 65;
            for( int col=1; col<=row; col++){
                System.out.print(ch);
                ch++;
            }
            System.out.println();
        }
    }
}
